<table cellspacing="0" cellpadding="0">
    <thead>
        <tr>
            <? for (var tagKey of statsData[0].tagKeys) { ?>
                <th class="borderRL">TagKey: <?= tagKey.name ?></th>
            <? } ?>
            <th class="borderRL">Sum, <?= view.measure.unit ?></th>
        </tr>
    </thead>
    <tbody>
        <? if (statsData[0].tagValues.length > 0) { ?>
            <? for (var data of statsData) { ?>
                <tr>
                    <? for (var tagValue of data.tagValues) { ?>
                        <? if (tagValue) { ?>
                            <td class="borderRL"><?= tagValue.value ?></td>
                        <? } else { ?>
                            <td class="borderRL"></td>
                        <? } ?>
                    <? } ?>
                <td class="borderRL"><?= data.snapshot.value ?></td>
                </tr>
            <? } ?>
        <? } ?>
    </tbody>
</table>
